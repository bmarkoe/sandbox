pipeline {
	agent any
	
	tools {
		jdk 'jdk12'
		maven 'maven3'
	}
	
	stages {
		stage('Install'){
		steps {
			sh "mvn clean test"
		}

	}
		stage('sonar'){
			steps{
			sh "mvn sonar:sonar -Dsonar.host.url=${env.SONARQUBE_HOST}"
			}
		}
}
}