pipeline {
	agent any
	
	tools {
		jdk 'jdk12'
		maven 'maven3'
	}
	
	stages {
		stage('Install'){
		steps {
			sh "mvn clean test"
		}
		post {
			always {
				junit allowEmptyResults: true, testResults: '**/target/*-reports/TEST-*.xml'
				}
		}
	}
}
}